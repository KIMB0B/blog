---
title: 🛠️ 블로그 대시보드
draft: true
---

> [!info] 이 문서는 관리 전용입니다
> vault 루트에 있어서 Quartz 빌드(`content/`만 빌드)에 포함되지 않아요. 사이트에는 절대 안 올라갑니다.
> 모든 목록은 Dataview가 실시간으로 계산하므로 **직접 수정할 필요가 없습니다.** 열 제목을 클릭하면 정렬도 됩니다.

# 📊 한눈에 보기

```dataviewjs
const posts = dv.pages('"content"')
  .where(p => p.file.name !== "index" && !p.file.name.includes("목차"));
const drafts = posts.where(p => p.draft === true);
const cutoff30 = dv.date("today").minus({ days: 30 });
const cutoff90 = dv.date("today").minus({ days: 90 });
const recent30 = posts.where(p => p.date && p.date >= cutoff30);
const recent90 = posts.where(p => p.date && p.date >= cutoff90);
const noTags = posts.where(p => !p.tags || p.tags.length === 0);
const noDate = posts.where(p => !p.date);

dv.table(["항목", "개수", "메모"], [
  ["📚 전체 글", posts.length, ""],
  ["🌐 공개 글", posts.length - drafts.length, "사이트에 올라가는 글"],
  ["✍️ 초안(draft)", drafts.length, drafts.length > 0 ? "아래 초안 섹션 확인" : "없음 👍"],
  ["🆕 최근 30일 작성", recent30.length, recent30.length === 0 ? "⚠️ 한 달간 새 글이 없어요!" : "순항 중 🚀"],
  ["🗓️ 최근 90일 작성", recent90.length, ""],
  ["🏷️ 태그 없는 글", noTags.length, noTags.length > 0 ? "점검 섹션 확인" : "없음 👍"],
  ["📅 날짜 없는 글", noDate.length, noDate.length > 0 ? "점검 섹션 확인" : "없음 👍"],
]);
```

---

# 🕘 최근 작성한 글

```dataview
TABLE WITHOUT ID
  file.link AS "글",
  dateformat(date, "yyyy-MM-dd") AS "작성일",
  join(tags, ", ") AS "태그"
FROM "content"
WHERE file.name != "index" AND !contains(file.name, "목차") AND date
SORT date DESC
LIMIT 15
```

---

# ✍️ 작성 중인 초안

> [!tip] 글을 완성하면 frontmatter에서 `draft: true`만 지우면 바로 배포돼요.

```dataview
TABLE WITHOUT ID
  file.link AS "글",
  dateformat(date, "yyyy-MM-dd") AS "만든 날",
  file.folder AS "위치"
FROM "content"
WHERE draft = true
SORT date DESC
```

---

# 🗂️ 카테고리별 현황

```dataviewjs
const posts = dv.pages('"content"')
  .where(p => p.file.name !== "index" && !p.file.name.includes("목차"));
const groups = posts.groupBy(p => p.file.folder.split("/")[1] ?? "기타");

dv.table(["카테고리", "글 수", "비중", "가장 최근 글"],
  groups
    .map(g => {
      const dated = g.rows.where(p => p.date).sort(p => p.date, "desc");
      const latest = dated.length > 0 ? dated[0] : null;
      const ratio = Math.round((g.rows.length / posts.length) * 100);
      return [
        g.key,
        g.rows.length,
        "▓".repeat(Math.max(1, Math.round(ratio / 4))) + ` ${ratio}%`,
        latest ? latest.file.link : "-",
      ];
    })
    .sort(r => r[1], "desc")
);
```

---

# 📅 월별 작성 추이

```dataviewjs
const posts = dv.pages('"content"')
  .where(p => p.date && p.file.name !== "index" && !p.file.name.includes("목차"));
const byMonth = {};
for (const p of posts) {
  const k = p.date.toFormat("yyyy-MM");
  byMonth[k] = (byMonth[k] ?? 0) + 1;
}
const keys = Object.keys(byMonth).sort().reverse().slice(0, 18);
dv.table(["월", "글 수", "추이"],
  keys.map(k => [k, byMonth[k], "█".repeat(Math.min(byMonth[k] * 2, 40))]));
```

---

# 🏷️ 태그별 글 수

```dataviewjs
const posts = dv.pages('"content"')
  .where(p => p.file.name !== "index" && !p.file.name.includes("목차"));
const counts = {};
for (const p of posts) {
  for (const t of (p.file.etags ?? [])) counts[t] = (counts[t] ?? 0) + 1;
}
dv.table(["태그", "글 수"],
  Object.entries(counts)
    .sort((a, b) => b[1] - a[1])
    .map(([t, c]) => [t, c]));
```

---

# 🧹 점검 필요

## 🏷️ 태그 없는 글

```dataview
LIST
FROM "content"
WHERE file.name != "index" AND !contains(file.name, "목차") AND !tags
```

## 📅 날짜 없는 글

> [!note] 폴더 index 페이지는 날짜가 없어도 무방해서 목록에서 제외했어요.

```dataview
LIST
FROM "content"
WHERE file.name != "index" AND !contains(file.name, "목차") AND !date
```

## 📏 내용이 너무 짧은 글 (미완성 의심)

```dataviewjs
const posts = dv.pages('"content"')
  .where(p => p.file.name !== "index" && !p.file.name.includes("목차"))
  .where(p => p.draft !== true && p.file.size < 600)
  .sort(p => p.file.size, "asc");
if (posts.length === 0) {
  dv.paragraph("✅ 없음 — 모든 공개 글이 충분한 분량이에요.");
} else {
  dv.table(["글", "크기(byte)", "위치"],
    posts.map(p => [p.file.link, p.file.size, p.file.folder]));
}
```

## 🔗 아무 글도 링크하지 않는 글 (고아 노트)

> [!note] 폴더 페이지의 자동 목록은 링크로 계산되지 않아서, 여기 나온다고 꼭 문제는 아니에요.
> 다만 관련 글끼리 `[[위키링크]]`로 이어주면 그래프도 풍성해지고 SEO/탐색에도 좋습니다.

```dataviewjs
const posts = dv.pages('"content"')
  .where(p => p.file.name !== "index" && !p.file.name.includes("목차"))
  .where(p => p.file.inlinks.length === 0)
  .sort(p => p.file.folder, "asc");
dv.table(["글", "위치"], posts.map(p => [p.file.link, p.file.folder]));
```

---

# ⏳ 오래된 글 리뷰 (작성 1년 경과)

> [!tip] 기술 글은 시간이 지나면 버전이나 내용이 낡을 수 있어요. 오래된 순서대로 가끔 훑어보세요.

```dataview
TABLE WITHOUT ID
  file.link AS "글",
  dateformat(date, "yyyy-MM-dd") AS "작성일",
  join(tags, ", ") AS "태그"
FROM "content"
WHERE file.name != "index" AND !contains(file.name, "목차")
  AND date AND date < date(today) - dur(365 days)
SORT date ASC
LIMIT 20
```

---

# 💡 글감 메모

> [!example] 여기에 자유롭게 아이디어를 적어두세요
> - [ ] 
> - [ ] 
> - [ ] 
